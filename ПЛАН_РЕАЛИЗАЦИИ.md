# 📋 ПЛАН ПОШАГОВОЙ РЕАЛИЗАЦИИ CryptoTracker

## 📌 О ЧЕМ ЭТОТ ПРОЕКТ

**CryptoTracker** — это веб-приложение для отслеживания криптовалют в реальном времени с использованием CoinGecko API.

### Что ты будешь уметь после этого проекта:
- ✅ Работать с внешним REST API без авторизации
- ✅ Использовать async/await для асинхронных запросов
- ✅ Создавать сложные DOM структуры динамически
- ✅ Работать с несколькими типами данных (список, портфель, графики)
- ✅ Интегрировать внешнюю библиотеку (Chart.js)
- ✅ Сохранять сложные данные в LocalStorage
- ✅ Управлять состоянием приложения
- ✅ Работать с табами и модальными окнами
- ✅ Делегирование событий для динамических элементов

### Технологии:
- **HTML5** — структура страницы (УЖЕ ГОТОВА)
- **CSS3** — стилизация (УЖЕ ГОТОВА)
- **Vanilla JavaScript** — вся логика (ЭТО ТЫ ПИШЕШЬ С НУЛЯ!)
- **CoinGecko API** — данные о криптовалютах (https://www.coingecko.com/en/api)
- **Chart.js** — графики цен (https://www.chartjs.org/)
- **LocalStorage** — хранение избранного и портфеля

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ ПЕРЕД НАЧАЛОМ

### Шаг 0: Подготовка

1. **Хорошая новость — API ключ НЕ НУЖЕН!**
   - CoinGecko API бесплатный и не требует регистрации
   - Есть лимит: 10-50 запросов в минуту (нам хватит)

2. **Структура проекта:**
   ```
   CryptoTracker/
   ├── index.html      (HTML разметка - УЖЕ ГОТОВ)
   ├── css/
   │   ├── variables.css    (CSS переменные - УЖЕ ГОТОВЫ)
   │   ├── style.css        (основные стили - УЖЕ ГОТОВЫ)
   │   └── components.css   (стили компонентов - УЖЕ ГОТОВЫ)
   └── js/
       ├── config.js        (ЭТО ТЫ ПИШЕШЬ!)
       ├── api.js          (ЭТО ТЫ ПИШЕШЬ!)
       ├── storage.js      (ЭТО ТЫ ПИШЕШЬ!)
       ├── ui.js           (ЭТО ТЫ ПИШЕШЬ!)
       ├── charts.js       (ЭТО ТЫ ПИШЕШЬ!)
       └── app.js          (ЭТО ТЫ ПИШЕШЬ!)
   ```

3. **Создай 6 пустых JS файлов** в папке `js/`

4. **Открой index.html** в браузере через Live Server

---

# 🚀 ЭТАП 1: КОНФИГУРАЦИЯ (config.js)

## 🎯 Цель:
Создать константы для работы с CoinGecko API и настройки приложения.

## 📋 Что нужно реализовать:

Тебе нужно создать и экспортировать константы для:

### 1. API настройки:
- **Базовый URL**: `https://api.coingecko.com/api/v3`
- **Объект с эндпоинтами**:
  - `markets`: `/coins/markets` — список криптовалют
  - `marketChart`: `/coins/{id}/market_chart` — данные для графика (заметь `{id}` — это плейсхолдер)
  - `search`: `/search` — поиск криптовалют

### 2. Параметры по умолчанию:
- Валюта по умолчанию: `'usd'`
- Сортировка по умолчанию: `'market_cap_desc'`
- Количество монет на странице: `50`

### 3. Поддерживаемые валюты (объект):
```
usd: { symbol: '$', name: 'US Dollar' }
eur: { symbol: '€', name: 'Euro' }
rub: { symbol: '₽', name: 'Russian Ruble' }
```

### 4. Периоды для графиков (объект):
```
1: '24 hours'
7: '7 days'
30: '30 days'
365: '1 year'
```

### 5. Ключи для LocalStorage (объект):
```
watchlist: 'crypto_tracker_watchlist'
portfolio: 'crypto_tracker_portfolio'
theme: 'crypto_tracker_theme'
currency: 'crypto_tracker_currency'
```

### 6. Интервал автообновления:
- В миллисекундах: `60000` (60 секунд)

### 7. Цвета для графиков (объект):
```
line: 'rgb(59, 130, 246)'
gradient1: 'rgba(59, 130, 246, 0.5)'
gradient2: 'rgba(59, 130, 246, 0)'
grid: 'rgba(0, 0, 0, 0.1)'
```

## 💡 Подсказки:

- Используй `export const` для экспорта каждой константы
- Называй константы ЗАГЛАВНЫМИ_БУКВАМИ (convention для констант)
- Для эндпоинтов используй объект с ключами и значениями
- Все константы должны быть в одном файле config.js

## 🔍 Как проверить:

Создай тестовый файл `test.html`:
```html
<script type="module">
  import { API_BASE_URL, CURRENCIES } from './js/config.js';
  console.log('Base URL:', API_BASE_URL);
  console.log('Валюты:', CURRENCIES);
  console.log('Символ доллара:', CURRENCIES.usd.symbol);
</script>
```

## 📊 Что ожидается:

- Файл `config.js` содержит все константы
- Каждая константа экспортируется через `export`
- Константы можно импортировать в другие модули
- Нет ошибок при загрузке модуля

## ❓ Вопросы для самопроверки:

1. Что такое ES6 модули? Зачем нужен `export`?
2. В чем разница между `export const` и `export default`?
3. Зачем выносить константы в отдельный файл?
4. Что такое плейсхолдер `{id}` в эндпоинте?
5. Почему названия констант пишутся ЗАГЛАВНЫМИ_БУКВАМИ?

## 🧪 Задания для экспериментов:

- Попробуй импортировать константы в консоли браузера
- Добавь свою валюту (например, GBP — фунт)
- Измени интервал обновления — как это повлияет?

## ☑️ Чек-лист готовности:

- [ ] Файл config.js создан
- [ ] Все 7 типов констант определены
- [ ] Все константы экспортируются
- [ ] Можно импортировать константы в другой файл
- [ ] Нет синтаксических ошибок
- [ ] Ты понимаешь зачем нужна каждая константа

---

# 🌐 ЭТАП 2: API МОДУЛЬ (api.js)

## 🎯 Цель:
Создать функции для получения данных с CoinGecko API.

## 📋 Что нужно реализовать:3

### Импорт:
Сначала импортируй нужные константы из config.js

### Функция 1: Базовый fetch запрос
**Название**: `fetchAPI` (можешь назвать по-своему)
**Что должна делать**:
- Принимать endpoint и объект параметров
- Создавать полный URL (базовый URL + endpoint)
- Добавлять параметры к URL через URLSearchParams
- Делать fetch запрос
- Проверять что response.ok === true
- Парсить JSON
- Возвращать данные
- Обрабатывать ошибки (try/catch)

**Подсказки**:
- Используй `new URL(baseUrl + endpoint)`
- Для параметров: `url.searchParams.append(key, value)`
- Проверяй `!response.ok` и выбрасывай ошибку
- Не забудь `await` для асинхронных операций

### Функция 2: Получить рыночные данные
**Название**: `getMarketData` (экспортируй!)
**Параметры**:
- currency (по умолчанию из констант)
- sort (по умолчанию из констант)
- page (по умолчанию 1)

**Что должна делать**:
- Формировать объект параметров:
  ```
  vs_currency: currency
  order: sort
  per_page: PER_PAGE константа
  page: page
  sparkline: false
  price_change_percentage: '24h,7d,30d'
  ```
- Вызывать fetchAPI с эндпоинтом markets
- Возвращать массив криптовалют

### Функция 3: Получить данные графика
**Название**: `getMarketChart` (экспортируй!)
**Параметры**:
- coinId (например 'bitcoin')
- currency (по умолчанию usd)
- days (по умолчанию 7)

**Что должна делать**:
- Заменить `{id}` в эндпоинте на реальный coinId
  - Подсказка: используй `.replace('{id}', coinId)`
- Формировать параметры:
  ```
  vs_currency: currency
  days: days
  ```
- Вызывать fetchAPI
- Возвращать данные графика

### Функция 4: Поиск криптовалют
**Название**: `searchCoins` (экспортируй!)
**Параметры**:
- query (поисковый запрос)

**Что должна делать**:
- Формировать параметры: `{ query: query }`
- Вызывать fetchAPI с эндпоинтом search
- Возвращать `data.coins` (массив результатов)

### Функция 5: Получить монеты по ID
**Название**: `getCoinsByIds` (экспортируй!)
**Параметры**:
- массив coinIds (например ['bitcoin', 'ethereum'])
- currency

**Что должна делать**:
- Проверить что массив не пустой (если пустой — вернуть [])
- Объединить ID в строку через запятую: `coinIds.join(',')`
- Формировать параметры:
  ```
  vs_currency: currency
  ids: строка_с_ids
  order: 'market_cap_desc'
  sparkline: false
  price_change_percentage: '24h'
  ```
- Вызывать fetchAPI с эндпоинтом markets
- Возвращать массив

## 💡 Подсказки:

- Все функции должны быть `async`
- Используй `await` для асинхронных операций
- Всегда оборачивай в try/catch
- URLSearchParams автоматически кодирует спецсимволы
- Для замены в строке: `str.replace('{id}', value)`

## 🔍 Как проверить:

**В консоли браузера:**
```javascript
import { getMarketData } from './js/api.js';

// Получи топ криптовалют
const coins = await getMarketData();
console.log('Получено монет:', coins.length);
console.log('Первая монета:', coins[0]);

// Структура должна быть:
// { id, symbol, name, image, current_price, market_cap, ... }
```

**Проверь график:**
```javascript
import { getMarketChart } from './js/api.js';

const chartData = await getMarketChart('bitcoin', 'usd', 7);
console.log('Данные графика:', chartData);
// Должны быть поля: prices, market_caps, total_volumes
// prices это массив: [[timestamp, price], [timestamp, price], ...]
```

**Проверь поиск:**
```javascript
import { searchCoins } from './js/api.js';

const results = await searchCoins('bitcoin');
console.log('Найдено:', results.length);
console.log('Первый результат:', results[0]);
```

## 📊 Что ожидается:

### Пример URL который должен сформироваться:
```
https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd&order=market_cap_desc&per_page=50&page=1&sparkline=false&price_change_percentage=24h,7d,30d
```

### Структура данных монеты:
```javascript
{
  id: "bitcoin",
  symbol: "btc",
  name: "Bitcoin",
  image: "https://...",
  current_price: 43250.50,
  market_cap: 847000000000,
  market_cap_rank: 1,
  total_volume: 25000000000,
  high_24h: 43500,
  low_24h: 42800,
  price_change_percentage_24h: 2.5
}
```

### Структура данных графика:
```javascript
{
  prices: [
    [1705305600000, 43100],
    [1705392000000, 43250],
    ...
  ],
  market_caps: [...],
  total_volumes: [...]
}
```

## 🎯 ПАРОВОЗИК: ПОТОК ДАННЫХ API

```
1️⃣ Вызов getMarketData('usd', 'market_cap_desc', 1)
          ↓
2️⃣ Формирование объекта параметров
   { vs_currency: 'usd', order: 'market_cap_desc', ... }
          ↓
3️⃣ Вызов fetchAPI('/coins/markets', params)
          ↓
4️⃣ Создание URL объекта
   new URL('https://api.coingecko.com/api/v3/coins/markets')
          ↓
5️⃣ Добавление параметров в URL
   url.searchParams.append('vs_currency', 'usd')
   url.searchParams.append('order', 'market_cap_desc')
   ...
          ↓
6️⃣ Полный URL готов:
   https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd&...
          ↓
7️⃣ fetch(url) отправляет GET запрос на CoinGecko
          ↓
8️⃣ Ожидание ответа (await)
          ↓
9️⃣ Получен response объект
          ↓
🔟 Проверка response.ok (статус 200-299?)
          ↓
1️⃣1️⃣ response.json() парсит JSON
          ↓
1️⃣2️⃣ Получен массив из 50 объектов-монет
          ↓
1️⃣3️⃣ Возврат данных в вызвавшую функцию
```

## ❓ Вопросы для самопроверки:

1. Что возвращает `fetch()`? Это сразу данные?
2. Зачем нужен `await` перед `fetch()`?
3. Что такое response.ok? Когда он false?
4. Зачем нужен второй `await` перед `.json()`?
5. Как добавить параметры в URL?
6. Что делает URLSearchParams?
7. Как заменить плейсхолдер в строке?
8. Зачем нужна функция-обертка fetchAPI?
9. Почему все функции async?
10. Что произойдет если не обработать ошибку?

## 🧪 Задания для экспериментов:

**Эксперимент 1: Изучи структуру данных**
```javascript
const coins = await getMarketData();
const firstCoin = coins[0];

console.log('ID:', firstCoin.id);
console.log('Название:', firstCoin.name);
console.log('Цена:', firstCoin.current_price);
console.log('Изменение:', firstCoin.price_change_percentage_24h);

// Какие еще поля есть в объекте?
console.log('Все поля:', Object.keys(firstCoin));
```

**Эксперимент 2: Попробуй разные параметры**
```javascript
// Топ 10 по объему торгов
const byVolume = await getMarketData('usd', 'volume_desc', 1);

// В евро вместо долларов
const inEuro = await getMarketData('eur', 'market_cap_desc', 1);

// Сравни цены
```

**Эксперимент 3: Проверь обработку ошибок**
```javascript
try {
  // Несуществующий эндпоинт
  await fetchAPI('/invalid/endpoint', {});
} catch (error) {
  console.error('Поймана ошибка:', error.message);
}
```

**Эксперимент 4: Посмотри сырой ответ**
```javascript
const response = await fetch('https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd&per_page=5');
console.log('Status:', response.status);
console.log('OK?:', response.ok);
console.log('Headers:', response.headers);

const data = await response.json();
console.log('Данные:', data);
```

**Эксперимент 5: График разных монет**
```javascript
const btcChart = await getMarketChart('bitcoin', 'usd', 7);
const ethChart = await getMarketChart('ethereum', 'usd', 7);

console.log('Bitcoin цены:', btcChart.prices.length);
console.log('Ethereum цены:', ethChart.prices.length);

// Сколько точек данных?
```

## ☑️ Чек-лист готовности:

- [ ] Файл api.js создан
- [ ] Импортированы константы из config.js
- [ ] Функция fetchAPI написана и работает
- [ ] Функция getMarketData возвращает массив монет
- [ ] Функция getMarketChart возвращает данные графика
- [ ] Функция searchCoins возвращает результаты поиска
- [ ] Функция getCoinsByIds работает с массивом ID
- [ ] Все функции экспортируются
- [ ] Обработка ошибок работает (try/catch)
- [ ] Ты понимаешь весь путь от вызова до получения данных

---

# 💾 ЭТАП 3: LOCALSTORAGE МОДУЛЬ (storage.js)

## 🎯 Цель:
Создать систему сохранения данных в браузере: избранное (watchlist) и портфель.

## 📋 Что нужно реализовать:

### Импорт:
Импортируй `STORAGE_KEYS` из config.js

### Вспомогательные функции (не экспортируй их!):

**Функция 1: Получить из хранилища**
**Название**: `getFromStorage`
**Параметры**: key, defaultValue
**Что делает**:
- try/catch обязателен!
- Читает `localStorage.getItem(key)`
- Если null — возвращает defaultValue
- Иначе парсит JSON: `JSON.parse(item)`
- При ошибке возвращает defaultValue

**Функция 2: Сохранить в хранилище**
**Название**: `saveToStorage`
**Параметры**: key, value
**Что делает**:
- try/catch обязателен!
- Превращает value в JSON: `JSON.stringify(value)`
- Сохраняет: `localStorage.setItem(key, jsonString)`
- Возвращает true при успехе, false при ошибке

### Объект Watchlist (экспортируй!)

Создай объект с методами:

**Метод get()**:
- Вызывает getFromStorage с ключом watchlist
- По умолчанию возвращает пустой массив []
- Возвращает массив ID монет: `['bitcoin', 'ethereum', ...]`

**Метод add(coinId)**:
- Получает текущий список (вызов get())
- Проверяет что coinId еще нет в списке
- Если нет — добавляет в массив (.push)
- Сохраняет обновленный массив
- Возвращает обновленный массив

**Метод remove(coinId)**:
- Получает текущий список
- Фильтрует массив, убирая coinId (.filter)
- Сохраняет отфильтрованный массив
- Возвращает массив

**Метод has(coinId)**:
- Получает текущий список
- Проверяет наличие: `.includes(coinId)`
- Возвращает true или false

**Метод clear()**:
- Сохраняет пустой массив []

### Объект Portfolio (экспортируй!)

Структура элемента портфеля:
```javascript
{
  id: 'bitcoin',
  name: 'Bitcoin',
  symbol: 'btc',
  image: 'https://...',
  amount: 1.5,          // количество монет
  buyPrice: 40000,      // цена покупки
  addedAt: 1705305600000  // timestamp
}
```

**Метод get()**:
- Аналогично watchlist.get()
- Возвращает массив объектов портфеля

**Метод add(coin)**:
- Получает текущий список
- Ищет монету по id: `.findIndex(item => item.id === coin.id)`
- Если найдена (index !== -1):
  - Обновляет amount: складывает старое и новое количество
  - Можно обновить buyPrice
- Если не найдена:
  - Создает новый объект с полями: id, name, symbol, image, amount, buyPrice, addedAt
  - Добавляет в массив
- Сохраняет массив
- Возвращает массив

**Метод remove(coinId)**:
- Получает список
- Фильтрует: `.filter(item => item.id !== coinId)`
- Сохраняет
- Возвращает массив

**Метод update(coinId, updates)**:
- Получает список
- Находит индекс монеты
- Обновляет: `list[index] = { ...list[index], ...updates }`
- Сохраняет
- Возвращает массив

**Метод has(coinId)**:
- Проверяет: `.some(item => item.id === coinId)`
- Возвращает boolean

**Метод clear()**:
- Сохраняет []

### Объект Settings (экспортируй!)

**Метод getTheme()**:
- Читает из LocalStorage
- По умолчанию 'light'

**Метод setTheme(theme)**:
- Сохраняет тему

**Метод getCurrency()**:
- Читает валюту
- По умолчанию 'usd'

**Метод setCurrency(currency)**:
- Сохраняет валюту

## 💡 Подсказки:

- LocalStorage хранит только строки!
- JSON.stringify превращает объект/массив в строку
- JSON.parse превращает строку обратно
- Используй try/catch для безопасности
- Методы объекта пишутся так: `methodName() { ... }`
- `this.get()` вызовет метод get того же объекта

## 🔍 Как проверить:

**Тест watchlist:**
```javascript
import { watchlist } from './js/storage.js';

// Добавь монеты
watchlist.add('bitcoin');
watchlist.add('ethereum');
watchlist.add('cardano');

// Получи список
const list = watchlist.get();
console.log('Список:', list);
// Должно быть: ['bitcoin', 'ethereum', 'cardano']

// Проверь наличие
console.log('Есть bitcoin?', watchlist.has('bitcoin')); // true
console.log('Есть dogecoin?', watchlist.has('dogecoin')); // false

// Удали
watchlist.remove('ethereum');
console.log('После удаления:', watchlist.get());
// Должно быть: ['bitcoin', 'cardano']

// Перезагрузи страницу!
// Список должен сохраниться!
```

**Тест portfolio:**
```javascript
import { portfolio } from './js/storage.js';

// Добавь монету
portfolio.add({
  id: 'bitcoin',
  name: 'Bitcoin',
  symbol: 'btc',
  image: 'https://example.com/btc.png',
  amount: 0.5,
  buyPrice: 40000,
  currentPrice: 43000
});

// Добавь еще раз (должна обновиться)
portfolio.add({
  id: 'bitcoin',
  amount: 0.3,
  buyPrice: 42000
});

const list = portfolio.get();
console.log('Портфель:', list);
// amount должен быть 0.8 (0.5 + 0.3)

// Проверь
console.log('В портфеле?', portfolio.has('bitcoin')); // true

// Удали
portfolio.remove('bitcoin');
console.log('После удаления:', portfolio.get());
// []
```

**Посмотри в DevTools:**
- F12 → Application → Local Storage
- Найди ключи: `crypto_tracker_watchlist`, `crypto_tracker_portfolio`
- Посмотри значения — это JSON строки

## 📊 Что ожидается:

### Watchlist в LocalStorage:
```json
Ключ: "crypto_tracker_watchlist"
Значение: '["bitcoin","ethereum","cardano"]'
```

### Portfolio в LocalStorage:
```json
Ключ: "crypto_tracker_portfolio"
Значение: '[{"id":"bitcoin","name":"Bitcoin","amount":0.5,"buyPrice":40000,...}]'
```

## 🎯 ПАРОВОЗИК: ДОБАВЛЕНИЕ В ПОРТФЕЛЬ

```
1️⃣ Пользователь добавляет Bitcoin, 0.5 BTC, $40k
          ↓
2️⃣ Вызов portfolio.add({ id: 'bitcoin', amount: 0.5, ... })
          ↓
3️⃣ Метод get() читает из LocalStorage
   localStorage.getItem('crypto_tracker_portfolio')
          ↓
4️⃣ Получена JSON строка (или null если первый раз)
   '[{"id":"ethereum","amount":1,...}]'
          ↓
5️⃣ JSON.parse превращает в массив JavaScript
   [ { id: 'ethereum', amount: 1 } ]
          ↓
6️⃣ Ищем: есть ли bitcoin в массиве?
   .findIndex(item => item.id === 'bitcoin')
          ↓
7️⃣ Не найден (index = -1)
          ↓
8️⃣ Создаем новый объект:
   { id: 'bitcoin', name: 'Bitcoin', amount: 0.5, buyPrice: 40000, addedAt: Date.now() }
          ↓
9️⃣ Добавляем в массив:
   [ { id: 'ethereum' }, { id: 'bitcoin' } ]
          ↓
🔟 JSON.stringify превращает в строку:
   '[{"id":"ethereum",...},{"id":"bitcoin",...}]'
          ↓
1️⃣1️⃣ localStorage.setItem сохраняет
          ↓
1️⃣2️⃣ Данные в браузере! Переживут перезагрузку!
```

## ❓ Вопросы для самопроверки:

1. Почему LocalStorage хранит только строки?
2. Что делает JSON.stringify? Когда его использовать?
3. Что делает JSON.parse? Что будет если передать невалидный JSON?
4. Зачем try/catch в функциях чтения/записи?
5. В чем разница между .includes() и .some()?
6. Что такое spread оператор `...`? Как он работает?
7. Зачем проверять наличие перед добавлением?
8. Что вернет .findIndex если не найдет элемент?
9. Можно ли сохранить в LocalStorage объект напрямую?
10. Что произойдет с данными при закрытии браузера?

## 🧪 Задания для экспериментов:

**Эксперимент 1: Массовое добавление**
```javascript
const coins = ['bitcoin', 'ethereum', 'cardano', 'ripple', 'polkadot'];
coins.forEach(coin => watchlist.add(coin));

console.log('Добавлено:', watchlist.get().length); // 5
```

**Эксперимент 2: Попробуй дубликат**
```javascript
watchlist.add('bitcoin');
watchlist.add('bitcoin');
watchlist.add('bitcoin');

console.log(watchlist.get());
// Должен быть только один 'bitcoin'!
```

**Эксперимент 3: Посмотри сырые данные**
```javascript
watchlist.add('bitcoin');

const raw = localStorage.getItem('crypto_tracker_watchlist');
console.log('Сырые данные:', raw);
console.log('Тип:', typeof raw);

const parsed = JSON.parse(raw);
console.log('Распарсенные:', parsed);
console.log('Тип:', typeof parsed);
```

**Эксперимент 4: Проверь обновление amount**
```javascript
portfolio.add({ id: 'bitcoin', amount: 1, buyPrice: 40000 });
portfolio.add({ id: 'bitcoin', amount: 0.5, buyPrice: 42000 });

const list = portfolio.get();
const btc = list.find(item => item.id === 'bitcoin');
console.log('Итоговое количество:', btc.amount);
// Должно быть 1.5
```

**Эксперимент 5: Очистка**
```javascript
// Добавь данные
watchlist.add('bitcoin');
portfolio.add({ id: 'ethereum', amount: 1 });

// Очисти
watchlist.clear();
portfolio.clear();

// Проверь
console.log('Watchlist:', watchlist.get()); // []
console.log('Portfolio:', portfolio.get()); // []

// Посмотри в DevTools - ключи должны быть пустыми массивами
```

## ☑️ Чек-лист готовности:

- [ ] Файл storage.js создан
- [ ] Импортированы STORAGE_KEYS
- [ ] Функции getFromStorage и saveToStorage работают
- [ ] Объект watchlist со всеми методами создан и экспортирован
- [ ] Объект portfolio со всеми методами создан и экспортирован
- [ ] Объект settings создан и экспортирован
- [ ] Дубликаты не добавляются
- [ ] Данные сохраняются в LocalStorage
- [ ] Данные переживают перезагрузку страницы
- [ ] Ты понимаешь как работает сериализация/десериализация

---

# 📊 ЭТАП 4: ГРАФИКИ (charts.js)

## 🎯 Цель:
Создать функции для работы с Chart.js — отрисовка интерактивных графиков цен.

## 📋 Что нужно реализовать:

### Импорт:
Импортируй `CHART_COLORS` из config.js

### Переменная для хранения графика:
Создай переменную `chartInstance = null` (не экспортируй)
Она будет хранить текущий график Chart.js

### Функция 1: Создать график
**Название**: `createPriceChart` (экспортируй!)
**Параметры**:
- canvasId (id элемента canvas, например 'priceChart')
- data (массив точек: [[timestamp, price], [timestamp, price], ...])
- label (название для легенды, по умолчанию 'Price')

**Что должна делать**:

1. **Получить canvas элемент**:
   - `document.getElementById(canvasId)`
   - Если не найден — вывести ошибку и вернуть null

2. **Получить 2D контекст**:
   - `canvas.getContext('2d')`

3. **Уничтожить старый график**:
   - Если `chartInstance` не null:
     - Вызвать `chartInstance.destroy()`
   - Это важно! Иначе будут накладываться графики

4. **Подготовить данные для Chart.js**:
   - **labels** (подписи оси X):
     - Преобразуй массив data: возьми timestamp из каждой точки
     - Создай Date объект: `new Date(point[0])`
     - Отформатируй дату используя `toLocaleDateString('ru-RU', options)`
     - Опции: month: 'short', day: 'numeric'
     - Если данных <= 24 точек (почасовой график): добавь hour и minute

   - **prices** (значения оси Y):
     - Преобразуй массив data: возьми цену из каждой точки `point[1]`

5. **Создать градиент**:
   - `ctx.createLinearGradient(0, 0, 0, 400)`
   - Добавь цвета из CHART_COLORS: gradient1 и gradient2
   - Используй `.addColorStop(0, color)` и `.addColorStop(1, color)`

6. **Создать график**:
   ```javascript
   chartInstance = new Chart(ctx, {
     type: 'line',
     data: {
       labels: labels,  // твой массив подписей
       datasets: [{
         label: label,
         data: prices,   // твой массив цен
         borderColor: CHART_COLORS.line,
         backgroundColor: gradient,
         borderWidth: 2,
         fill: true,
         tension: 0.4,   // плавность линии
         pointRadius: 0,  // без точек на линии
         pointHoverRadius: 6,  // но при наведении показать
         pointHoverBackgroundColor: CHART_COLORS.line,
         pointHoverBorderColor: '#fff',
         pointHoverBorderWidth: 2
       }]
     },
     options: {
       responsive: true,
       maintainAspectRatio: false,
       interaction: { intersect: false, mode: 'index' },
       plugins: {
         legend: { display: false },
         tooltip: {
           backgroundColor: 'rgba(0, 0, 0, 0.8)',
           padding: 12,
           borderColor: CHART_COLORS.line,
           borderWidth: 1,
           displayColors: false,
           callbacks: {
             label: function(context) {
               // Форматируй цену с $ и запятыми
               return '$' + context.parsed.y.toLocaleString('en-US');
             }
           }
         }
       },
       scales: {
         x: {
           grid: { display: false },
           ticks: { maxTicksLimit: 8, color: '#6b7280' }
         },
         y: {
           grid: { color: CHART_COLORS.grid },
           ticks: {
             color: '#6b7280',
             callback: function(value) {
               return '$' + value.toLocaleString('en-US');
             }
           }
         }
       }
     }
   });
   ```

7. **Вернуть chartInstance**

### Функция 2: Обновить график
**Название**: `updateChart` (экспортируй!)
**Параметры**:
- data (новые данные)
- label (новое название)

**Что должна делать**:
- Проверить что chartInstance существует
- Подготовить новые labels и prices (как в createPriceChart)
- Обновить данные:
  - `chartInstance.data.labels = labels`
  - `chartInstance.data.datasets[0].data = prices`
  - `chartInstance.data.datasets[0].label = label`
- Вызвать `chartInstance.update()` для перерисовки

### Функция 3: Уничтожить график
**Название**: `destroyChart` (экспортируй!)
**Что должна делать**:
- Проверить что chartInstance существует
- Вызвать `chartInstance.destroy()`
- Установить `chartInstance = null`

### Функция 4: Получить статистику
**Название**: `getChartStats` (экспортируй!)
**Параметры**: data (массив точек)
**Что должна делать**:
- Проверить что data не пустой
- Получить массив только цен: `data.map(point => point[1])`
- Вычислить:
  - `highest`: максимальная цена (Math.max)
  - `lowest`: минимальная цена (Math.min)
  - `first`: первая цена
  - `last`: последняя цена
  - `change`: разница между last и first
  - `changePercent`: (change / first) * 100
- Вернуть объект со всеми этими полями

## 💡 Подсказки:

- Chart.js создается через `new Chart(context, config)`
- Обязательно уничтожай старый график перед созданием нового
- `.map()` преобразует массив
- `Math.max(...array)` находит максимум (используй spread ...)
- toLocaleString форматирует числа с разделителями

## 🔍 Как проверить:

**Получи данные и создай график:**
```javascript
import { getMarketChart } from './js/api.js';
import { createPriceChart, getChartStats } from './js/charts.js';

// Получи данные
const chartData = await getMarketChart('bitcoin', 'usd', 7);
console.log('Точек данных:', chartData.prices.length);

// Создай график
const chart = createPriceChart('priceChart', chartData.prices, 'Bitcoin Price');

// График должен появиться на странице!
```

**Проверь статистику:**
```javascript
const stats = getChartStats(chartData.prices);
console.log('Макс цена:', stats.highest);
console.log('Мин цена:', stats.lowest);
console.log('Изменение:', stats.change);
console.log('Процент:', stats.changePercent);
```

**Обновление графика:**
```javascript
// Получи данные для другого периода
const data30d = await getMarketChart('bitcoin', 'usd', 30);

// Обнови тот же график
updateChart(data30d.prices, 'Bitcoin 30 days');

// График должен перерисоваться!
```

## 📊 Что ожидается:

### Формат входных данных:
```javascript
[
  [1705305600000, 43100.50],  // [timestamp, price]
  [1705392000000, 43250.25],
  [1705478400000, 42980.75],
  ...
]
```

### После преобразования для Chart.js:
```javascript
labels: ['15 янв', '16 янв', '17 янв', ...]
prices: [43100.50, 43250.25, 42980.75, ...]
```

### Объект статистики:
```javascript
{
  highest: 43500,
  lowest: 42800,
  first: 43100,
  last: 43250,
  change: 150,
  changePercent: 0.35
}
```

## 🎯 ПАРОВОЗИК: ОТ ДАННЫХ API ДО ГРАФИКА

```
1️⃣ Вызов getMarketChart('bitcoin', 'usd', 7)
          ↓
2️⃣ API возвращает объект с полем prices:
   { prices: [[timestamp, price], ...] }
          ↓
3️⃣ Вызов createPriceChart('priceChart', data.prices, 'Bitcoin')
          ↓
4️⃣ Получение canvas элемента из DOM
   const canvas = document.getElementById('priceChart')
          ↓
5️⃣ Получение 2D контекста для рисования
   const ctx = canvas.getContext('2d')
          ↓
6️⃣ Преобразование данных:
   [[1705305600000, 43100], ...]
          ↓
   labels: ['15 янв', '16 янв', ...]
   prices: [43100, 43250, ...]
          ↓
7️⃣ Создание градиента для заливки
   ctx.createLinearGradient(0, 0, 0, 400)
          ↓
8️⃣ Создание Chart.js инстанса
   new Chart(ctx, { type: 'line', data: {...}, options: {...} })
          ↓
9️⃣ Chart.js рисует график на canvas
          ↓
🔟 Пользователь видит красивый график цены! 📈
```

## ❓ Вопросы для самопроверки:

1. Что такое canvas? Для чего он используется?
2. Что такое 2D контекст?
3. Зачем уничтожать старый график?
4. Что делает .map() с массивом?
5. Как работает spread оператор `...` с Math.max?
6. Что такое gradient и как его создать?
7. Зачем нужен tension в настройках линии?
8. Что делает toLocaleString?
9. Можно ли создать несколько графиков одновременно?
10. Что произойдет если забыть вызвать chart.destroy()?

## 🧪 Задания для экспериментов:

**Эксперимент 1: Графики разных монет**
```javascript
// Bitcoin
const btcData = await getMarketChart('bitcoin', 'usd', 7);
createPriceChart('priceChart', btcData.prices, 'Bitcoin');

// Подожди 3 секунды

// Ethereum (график обновится)
const ethData = await getMarketChart('ethereum', 'usd', 7);
createPriceChart('priceChart', ethData.prices, 'Ethereum');
```

**Эксперимент 2: Разные периоды**
```javascript
const periods = [1, 7, 30, 365];

for (const days of periods) {
  const data = await getMarketChart('bitcoin', 'usd', days);
  createPriceChart('priceChart', data.prices, `Bitcoin ${days}d`);
  await new Promise(resolve => setTimeout(resolve, 3000)); // Пауза 3 сек
}
```

**Эксперимент 3: Статистика**
```javascript
const data = await getMarketChart('bitcoin', 'usd', 30);
const stats = getChartStats(data.prices);

console.log(`
  За последние 30 дней:
  Максимум: $${stats.highest}
  Минимум: $${stats.lowest}
  Изменение: $${stats.change} (${stats.changePercent.toFixed(2)}%)
`);
```

**Эксперимент 4: Проверь формат дат**
```javascript
const data = await getMarketChart('bitcoin', 'usd', 1); // 24 часа
createPriceChart('priceChart', data.prices, 'Bitcoin 24h');
// Должны быть часы в подписях: "15 янв, 10:00"

const data7d = await getMarketChart('bitcoin', 'usd', 7);
createPriceChart('priceChart', data7d.prices, 'Bitcoin 7d');
// Только даты: "15 янв", "16 янв"
```

## ☑️ Чек-лист готовности:

- [ ] Файл charts.js создан
- [ ] Импортированы CHART_COLORS
- [ ] Переменная chartInstance объявлена
- [ ] Функция createPriceChart создает график
- [ ] График отображается на странице
- [ ] Даты форматируются правильно
- [ ] Градиент работает
- [ ] Функция updateChart обновляет график
- [ ] Функция destroyChart уничтожает график
- [ ] Функция getChartStats вычисляет статистику
- [ ] Все функции экспортируются
- [ ] Ты понимаешь как данные превращаются в визуальный график

---

# 🎨 ЭТАП 5: UI МОДУЛЬ (ui.js)

## 🎯 Цель:
Создать функции для отображения интерфейса: форматирование, создание карточек, рендеринг списков.

## 📋 Что нужно реализовать:

### Импорты:
```javascript
import { CURRENCIES } from './config.js';
import { watchlist } from './storage.js';
```

### БЛОК 1: Функции форматирования

**Функция 1: Форматировать цену**
**Название**: `formatPrice` (экспортируй!)
**Параметры**: price, currency (по умолчанию 'usd')
**Что делает**:
- Проверяет что price не null/undefined
- Если null — возвращает 'N/A'
- Получает символ валюты из CURRENCIES[currency].symbol
- Если цена < 0.01: форматирует с 8 знаками после запятой
- Если цена < 1: форматирует с 4 знаками
- Иначе: форматирует с 2 знаками и разделителями (toLocaleString)
- Возвращает строку: '$43,250.50'

**Функция 2: Форматировать большие числа**
**Название**: `formatLargeNumber` (экспортируй!)
**Параметры**: number
**Что делает**:
- Если null — возвращает 'N/A'
- Если >= 1 триллион (1e12): делит на 1e12, добавляет 'T'
- Если >= 1 миллиард (1e9): делит на 1e9, добавляет 'B'
- Если >= 1 миллион (1e6): делит на 1e6, добавляет 'M'
- Если >= 1 тысяча (1e3): делит на 1e3, добавляет 'K'
- Иначе: просто число
- Примеры: $2.5T, $847B, $1.2M

**Функция 3: Форматировать процент**
**Название**: `formatPercent` (экспортируй!)
**Параметры**: percent
**Что делает**:
- Если null — 'N/A'
- Добавляет знак '+' если >= 0
- Форматирует с 2 знаками после запятой
- Добавляет '%'
- Примеры: '+2.50%', '-1.25%'

**Функция 4: Получить класс для процента**
**Название**: `getPercentClass` (экспортируй!)
**Параметры**: percent
**Что делает**:
- Если >= 0: возвращает 'positive'
- Иначе: возвращает 'negative'
- Используется для CSS классов (зеленый/красный)

### БЛОК 2: Создание элементов

**Функция 5: Создать карточку криптовалюты**
**Название**: `createCryptoCard` (экспортируй!)
**Параметры**: coin (объект с данными монеты), currency
**Что делает**:

1. Создать контейнер:
   - `document.createElement('div')`
   - Класс: 'crypto-card'
   - dataset.coinId = coin.id

2. Проверить избранное:
   - `const isFavorite = watchlist.has(coin.id)`

3. Установить innerHTML карточки:
```html
<div class="crypto-card-header">
  <img src="..." class="crypto-icon">
  <div class="crypto-info">
    <div class="crypto-name">Bitcoin</div>
    <div class="crypto-symbol">btc</div>
  </div>
  <span class="crypto-rank">#1</span>
</div>

<div class="crypto-card-body">
  <div class="crypto-price">$43,250.50</div>
  <span class="crypto-change positive">+2.50%</span>

  <div class="crypto-stats">
    <div class="crypto-stat">
      <span class="crypto-stat-label">Market Cap</span>
      <span class="crypto-stat-value">$847B</span>
    </div>
    <div class="crypto-stat">
      <span class="crypto-stat-label">Volume 24h</span>
      <span class="crypto-stat-value">$25B</span>
    </div>
    <div class="crypto-stat">
      <span class="crypto-stat-label">Highest 24h</span>
      <span class="crypto-stat-value">$43,500</span>
    </div>
    <div class="crypto-stat">
      <span class="crypto-stat-label">Lowest 24h</span>
      <span class="crypto-stat-value">$42,800</span>
    </div>
  </div>
</div>

<div class="crypto-card-footer">
  <button class="btn-favorite active" data-coin-id="bitcoin">
    ⭐  (или ☆ если не в избранном)
  </button>
  <button class="btn-card" data-action="chart" data-coin-id="bitcoin">
    📈 График
  </button>
  <button class="btn-card primary" data-action="add-portfolio" data-coin-id="bitcoin">
    💼 Портфель
  </button>
</div>
```

Используй функции форматирования!

4. Вернуть DOM элемент

**Функция 6: Отобразить список криптовалют**
**Название**: `renderCryptoList` (экспортируй!)
**Параметры**: coins (массив), containerId, currency
**Что делает**:
- Получить контейнер по ID
- Если контейнер не найден — выйти
- Если массив пустой:
  - Показать сообщение "Нет данных"
  - Выйти
- Очистить контейнер: `container.innerHTML = ''`
- Пройти по массиву монет (.forEach)
- Для каждой создать карточку (createCryptoCard)
- Добавить карточку в контейнер (.appendChild)

**Функция 7: Показать loading**
**Название**: `showLoading` (экспортируй!)
**Параметры**: containerId
**Что делает**:
- Получить контейнер
- Установить innerHTML:
```html
<div class="loading">
  <div class="spinner"></div>
</div>
```

**Функция 8: Показать ошибку**
**Название**: `showError` (экспортируй!)
**Параметры**: containerId, message
**Что делает**:
- Установить innerHTML:
```html
<div class="empty-state">
  <span class="empty-icon">⚠️</span>
  <p>текст_ошибки</p>
</div>
```

**Функция 9: Создать элемент результата поиска**
**Название**: `createSearchResultItem` (экспортируй!)
**Параметры**: coin
**Что делает**:
- Создать div с классом 'search-result-item'
- dataset.coinId = coin.id
- innerHTML:
```html
<img src="..." class="search-result-icon">
<div class="search-result-info">
  <div class="search-result-name">Bitcoin</div>
  <div class="search-result-symbol">BTC</div>
</div>
<span class="crypto-rank">#1</span>
```
- Вернуть элемент

### БЛОК 3: Управление UI

**Функция 10: Переключить модалку**
**Название**: `toggleModal` (экспортируй!)
**Параметры**: modalId, show (boolean)
**Что делает**:
- Получить модалку по ID
- Если show === true:
  - Добавить класс 'show'
  - Заблокировать скролл: `document.body.style.overflow = 'hidden'`
- Иначе:
  - Убрать класс 'show'
  - Разблокировать: `document.body.style.overflow = ''`

**Функция 11: Обновить статистику в header**
**Название**: `updateHeaderStats` (экспортируй!)
**Параметры**: totalCryptos, portfolioValue
**Что делает**:
- Найти элементы 'totalCryptos' и 'portfolioValue'
- Установить textContent

**Функция 12: Обновить счетчик избранного**
**Название**: `updateWatchlistCount` (экспортируй!)
**Параметры**: count
**Что делает**:
- Найти элемент 'watchlistCount'
- Установить textContent = count

**Функция 13: Переключить таб**
**Название**: `switchTab` (экспортируй!)
**Параметры**: tabName ('market', 'watchlist', 'portfolio')
**Что делает**:
- Убрать класс 'active' у всех .tab-btn
- Убрать класс 'active' у всех .tab-pane
- Найти кнопку: `[data-tab="${tabName}"]`
- Найти панель: `${tabName}Tab`
- Добавить класс 'active' обоим

### БЛОК 4: Портфель

**Функция 14: Создать элемент портфеля**
**Название**: `createPortfolioItem` (экспортируй!)
**Параметры**: item, currentPrice
**Что делает**:
- Вычислить:
  - totalValue = amount * currentPrice
  - totalCost = amount * buyPrice
  - profit = totalValue - totalCost
  - profitPercent = (profit / totalCost) * 100
- Создать div.portfolio-item
- Заполнить данными (используй форматирование!)
- Показать иконку, название, количество, цены, прибыль
- Добавить кнопку удаления
- Вернуть элемент

**Функция 15: Отобразить портфель**
**Название**: `renderPortfolio` (экспортируй!)
**Параметры**: portfolioItems (массив), currentPrices (объект {coinId: price})
**Что делает**:
- Получить контейнер 'portfolioList' и 'portfolioEmpty'
- Если массив пустой:
  - Скрыть контейнер (класс 'hidden')
  - Показать empty state
  - Выйти
- Показать контейнер, скрыть empty
- Очистить контейнер
- Для каждого элемента:
  - Получить текущую цену из currentPrices
  - Создать элемент портфеля
  - Добавить в контейнер

**Функция 16: Обновить сводку портфеля**
**Название**: `updatePortfolioSummary` (экспортируй!)
**Параметры**: totalValue, totalCost, change24h
**Что делает**:
- Вычислить profit и profitPercent
- Обновить элементы:
  - 'totalValue': отформатированная стоимость
  - 'profitLoss': прибыль с процентом + класс (positive/negative)
  - 'change24h': процент изменения + класс

**Функция 17: Отобразить информацию графика**
**Название**: `renderChartInfo` (экспортируй!)
**Параметры**: stats (объект статистики)
**Что делает**:
- Получить контейнер 'chartInfo'
- Установить innerHTML с 3 блоками:
  - Максимум
  - Минимум
  - Изменение (с классом positive/negative)

## 💡 Подсказки:

- Используй template literals (обратные кавычки) для HTML
- Не забывай экранировать кавычки
- textContent безопаснее чем innerHTML для текста
- classList.add/remove для управления классами
- dataset для data-атрибутов

## 🔍 Как проверить:

**Тест форматирования:**
```javascript
import * as UI from './js/ui.js';

console.log(UI.formatPrice(43250.50, 'usd'));  // $43,250.50
console.log(UI.formatPrice(0.0012, 'usd'));    // $0.0012
console.log(UI.formatLargeNumber(847000000000)); // $847.00B
console.log(UI.formatPercent(2.5));             // +2.50%
console.log(UI.formatPercent(-1.25));           // -1.25%
console.log(UI.getPercentClass(2.5));           // positive
```

**Тест карточки:**
```javascript
import { getMarketData } from './js/api.js';

const coins = await getMarketData();
const card = UI.createCryptoCard(coins[0], 'usd');

console.log(card); // должен быть DOM элемент
document.getElementById('cryptoList').appendChild(card);
// Карточка должна появиться на странице!
```

**Тест списка:**
```javascript
const coins = await getMarketData();
UI.renderCryptoList(coins, 'cryptoList', 'usd');
// Должно появиться 50 карточек!
```

## ☑️ Чек-лист готовности:

- [ ] Файл ui.js создан
- [ ] Все функции форматирования работают
- [ ] createCryptoCard создает красивую карточку
- [ ] renderCryptoList отображает массив карточек
- [ ] Функции loading/error работают
- [ ] Модалки открываются/закрываются
- [ ] Табы переключаются
- [ ] Портфель отображается правильно
- [ ] Все функции экспортируются
- [ ] Ты понимаешь как данные превращаются в UI элементы

---

# ⚙️ ЭТАП 6: СОСТОЯНИЕ И ЗАГРУЗКА ДАННЫХ (app.js - Часть 1)

## 🎯 Цель:
Создать главный модуль приложения: состояние, инициализацию и функции загрузки данных.

## 📋 Что нужно реализовать:

### Импорты:
Импортируй ВСЕ необходимые модули:
```javascript
import * as API from './api.js';
import { watchlist, portfolio, settings } from './storage.js';
import * as UI from './ui.js';
import { createPriceChart, getChartStats, destroyChart } from './charts.js';
import { UPDATE_INTERVAL, DEFAULT_CURRENCY } from './config.js';
```

### 1. Объект состояния (state)

Создай объект для хранения состояния приложения:
```javascript
const state = {
  currentPage: 1,                           // текущая страница пагинации
  currentCurrency: 'usd',                   // текущая валюта
  currentSort: 'market_cap_desc',           // текущая сортировка
  marketData: [],                           // данные рынка
  watchlistData: [],                        // данные избранного
  portfolioData: [],                        // данные портфеля
  currentPrices: {},                        // объект {coinId: price} для кэша цен
  searchTimeout: null,                      // таймер для дебаунса поиска
  updateInterval: null,                     // интервал автообновления
  currentChartCoin: null,                   // какая монета открыта в графике
  currentChartPeriod: 7                     // период графика
};
```

**Зачем нужен state?**
- Централизованное хранение данных
- Легко отследить что изменилось
- Доступ из любой функции

### 2. Функция инициализации

**Название**: `init` (async)
**Что должна делать**:

1. Вывести в консоль: '🚀 CryptoTracker starting...'
2. Применить тему: вызвать функцию `applyTheme()` (её напишешь позже)
3. Установить валюту в state:
   - `state.currentCurrency = settings.getCurrency() || DEFAULT_CURRENCY`
4. Загрузить данные рынка: `await loadMarketData()`
5. Загрузить избранное: `await loadWatchlistData()`
6. Загрузить портфель: `await loadPortfolioData()`
7. Настроить обработчики: `setupEventListeners()` (её напишешь позже)
8. Запустить автообновление: `startAutoUpdate()` (её напишешь позже)
9. Вывести в консоль: '✅ CryptoTracker ready!'

**Подсказка**: Эта функция — точка входа, её нужно будет вызвать в самом конце файла!

### 3. Функция загрузки рынка

**Название**: `loadMarketData` (async)
**Что должна делать**:

1. try/catch обязателен!
2. Показать loading: `UI.showLoading('cryptoList')`
3. Получить данные:
   ```javascript
   const data = await API.getMarketData(
     state.currentCurrency,
     state.currentSort,
     state.currentPage
   );
   ```
4. Сохранить в state: `state.marketData = data`
5. Обновить кэш цен (пройти по массиву data):
   ```javascript
   data.forEach(coin => {
     state.currentPrices[coin.id] = coin.current_price;
   });
   ```
6. Отрендерить список:
   ```javascript
   UI.renderCryptoList(data, 'cryptoList', state.currentCurrency);
   ```
7. Обновить статистику в header:
   ```javascript
   UI.updateHeaderStats(data.length, calculatePortfolioValue());
   ```
8. При ошибке (catch):
   - Вывести ошибку в консоль
   - Показать UI.showError('cryptoList', 'Не удалось загрузить данные рынка')

### 4. Функция загрузки избранного

**Название**: `loadWatchlistData` (async)
**Что должна делать**:

1. try/catch
2. Получить список ID: `const watchlistIds = watchlist.get()`
3. Если массив пустой (length === 0):
   - Очистить state.watchlistData = []
   - Отрендерить пустой список: `UI.renderCryptoList([], 'watchlistList')`
   - Показать empty state: найти элемент 'watchlistEmpty', убрать класс 'hidden'
   - Обновить счетчик: `UI.updateWatchlistCount(0)`
   - Выйти из функции (return)
4. Если есть монеты:
   - Показать loading
   - Получить данные: `await API.getCoinsByIds(watchlistIds, state.currentCurrency)`
   - Сохранить в state.watchlistData
   - Обновить кэш цен (как в loadMarketData)
   - Скрыть empty state (добавить класс 'hidden')
   - Отрендерить список
   - Обновить счетчик
5. При ошибке — показать UI.showError

### 5. Функция загрузки портфеля

**Название**: `loadPortfolioData` (async)
**Что должна делать**:

1. try/catch
2. Получить элементы портфеля: `const portfolioItems = portfolio.get()`
3. Сохранить в state: `state.portfolioData = portfolioItems`
4. Если массив пустой:
   - Отрендерить пустой портфель: `UI.renderPortfolio([], {})`
   - Обновить сводку с нулями: `UI.updatePortfolioSummary(0, 0, 0)`
   - Выйти (return)
5. Если есть элементы:
   - Получить массив ID: `portfolioItems.map(item => item.id)`
   - Получить текущие данные: `await API.getCoinsByIds(coinIds, state.currentCurrency)`
   - Создать объект pricesMap (пустой объект {})
   - Переменная для суммы изменений: `let total24hChange = 0`
   - Пройти по currentData:
     - Для каждой монеты: `pricesMap[coin.id] = coin.current_price`
     - Обновить кэш: `state.currentPrices[coin.id] = coin.current_price`
     - Добавить к сумме: `total24hChange += coin.price_change_percentage_24h || 0`
   - Вычислить среднее изменение: `avg24hChange = total24hChange / currentData.length`
   - Отрендерить портфель: `UI.renderPortfolio(portfolioItems, pricesMap)`
   - Вычислить статистику: вызов `calculatePortfolioStats(portfolioItems, pricesMap)`
     - Получишь объект: `{ totalValue, totalCost }`
   - Обновить сводку: `UI.updatePortfolioSummary(totalValue, totalCost, avg24hChange)`
   - Обновить header: `UI.updateHeaderStats(state.marketData.length, totalValue)`
6. При ошибке — UI.showError

### 6. Вспомогательные функции

**Функция: Вычислить статистику портфеля**
**Название**: `calculatePortfolioStats`
**Параметры**: portfolioItems, pricesMap
**Что делает**:
- Создать переменные: `let totalValue = 0; let totalCost = 0;`
- Пройти по portfolioItems (.forEach):
  - Получить текущую цену: `pricesMap[item.id] || item.buyPrice`
  - Добавить к totalValue: `item.amount * currentPrice`
  - Добавить к totalCost: `item.amount * item.buyPrice`
- Вернуть объект: `{ totalValue, totalCost }`

**Функция: Вычислить общую стоимость портфеля**
**Название**: `calculatePortfolioValue`
**Что делает**:
- Получить портфель: `portfolio.get()`
- Если пустой — вернуть 0
- Создать переменную total = 0
- Пройти по элементам:
  - Получить цену из кэша или buyPrice
  - Добавить к total: amount * price
- Вернуть total

## 💡 Подсказки:

- Все функции загрузки данных асинхронные (async)
- Используй await для API вызовов
- Всегда оборачивай в try/catch
- Сначала показывай loading, потом данные
- Обновляй state перед рендерингом
- Кэш цен нужен чтобы не делать лишние запросы

## 🔍 Как проверить:

**Тест инициализации:**
```javascript
// В конце файла app.js вызови:
init();

// В консоли должно появиться:
// 🚀 CryptoTracker starting...
// ✅ CryptoTracker ready!

// На странице должны загрузиться монеты
```

**Тест state:**
```javascript
// После загрузки откройте консоль:
console.log('State:', state);

// Проверь поля:
console.log('Монет на рынке:', state.marketData.length); // 50
console.log('Текущая валюта:', state.currentCurrency);   // usd
console.log('Кэш цен:', state.currentPrices);           // объект с ценами
```

**Тест избранного:**
```javascript
import { watchlist } from './js/storage.js';

// Добавь монеты
watchlist.add('bitcoin');
watchlist.add('ethereum');

// Перезагрузи страницу
// Вкладка Watchlist должна показать 2 монеты!
```

**Тест портфеля:**
```javascript
import { portfolio } from './js/storage.js';

portfolio.add({
  id: 'bitcoin',
  name: 'Bitcoin',
  symbol: 'btc',
  amount: 0.5,
  buyPrice: 40000,
  image: 'https://...'
});

// Перезагрузи страницу
// Вкладка Portfolio должна показать позицию!
```

## 📊 Что ожидается:

### При запуске приложения:
1. Консоль: '🚀 CryptoTracker starting...'
2. Загружаются данные рынка (50 монет)
3. Загружаются данные избранного (если есть)
4. Загружается портфель (если есть)
5. Консоль: '✅ CryptoTracker ready!'

### Структура state после загрузки:
```javascript
{
  currentPage: 1,
  currentCurrency: 'usd',
  currentSort: 'market_cap_desc',
  marketData: [ {id: 'bitcoin', ...}, ... ],  // 50 монет
  watchlistData: [ {id: 'ethereum', ...} ],    // избранные
  portfolioData: [ {id: 'bitcoin', amount: 0.5, ...} ],
  currentPrices: {
    bitcoin: 43250.50,
    ethereum: 2285.75,
    ...
  },
  ...
}
```

## 🎯 ПАРОВОЗИК: ЗАПУСК ПРИЛОЖЕНИЯ

```
1️⃣ DOMContentLoaded или вызов init() в конце файла
          ↓
2️⃣ init() функция начинает работу
          ↓
3️⃣ applyTheme() — применяется тема из LocalStorage
          ↓
4️⃣ loadMarketData() вызывается
          ↓
5️⃣ UI.showLoading('cryptoList') — показывается спиннер
          ↓
6️⃣ API.getMarketData() — запрос к CoinGecko
          ↓
7️⃣ Получен массив из 50 монет
          ↓
8️⃣ state.marketData = data — сохранение в state
          ↓
9️⃣ Цикл по data: обновление state.currentPrices
          ↓
🔟 UI.renderCryptoList() — рендеринг 50 карточек
          ↓
1️⃣1️⃣ UI.updateHeaderStats() — обновление статистики
          ↓
1️⃣2️⃣ loadWatchlistData() вызывается
          ↓
1️⃣3️⃣ watchlist.get() — чтение из LocalStorage
          ↓
1️⃣4️⃣ Если есть ID: API.getCoinsByIds() — запрос данных
          ↓
1️⃣5️⃣ UI.renderCryptoList() для вкладки Watchlist
          ↓
1️⃣6️⃣ loadPortfolioData() вызывается
          ↓
1️⃣7️⃣ portfolio.get() — чтение портфеля
          ↓
1️⃣8️⃣ API.getCoinsByIds() — текущие цены для портфеля
          ↓
1️⃣9️⃣ calculatePortfolioStats() — расчет прибыли/убытков
          ↓
2️⃣0️⃣ UI.renderPortfolio() + UI.updatePortfolioSummary()
          ↓
2️⃣1️⃣ setupEventListeners() — настройка обработчиков
          ↓
2️⃣2️⃣ startAutoUpdate() — запуск автообновления каждые 60 сек
          ↓
2️⃣3️⃣ Приложение готово! ✅
```

## ❓ Вопросы для самопроверки:

1. Зачем нужен объект state?
2. Почему функция init должна быть async?
3. Зачем кэшировать цены в state.currentPrices?
4. Что произойдет если не показать loading перед запросом?
5. Почему нужно обновлять header stats после каждой загрузки?
6. В чем разница между marketData и watchlistData?
7. Зачем проверять пустой массив перед рендерингом?
8. Что такое avg24hChange и как его вычислить?
9. Почему totalValue и totalCost разные?
10. Что будет если забыть try/catch?

## 🧪 Задания для экспериментов:

**Эксперимент 1: Следи за state в реальном времени**
```javascript
// После загрузки приложения
setInterval(() => {
  console.clear();
  console.log('📊 State snapshot:', {
    монет_на_рынке: state.marketData.length,
    в_избранном: state.watchlistData.length,
    в_портфеле: state.portfolioData.length,
    цен_в_кэше: Object.keys(state.currentPrices).length,
    валюта: state.currentCurrency
  });
}, 5000);
```

**Эксперимент 2: Проверь как работает кэш цен**
```javascript
console.log('Bitcoin цена:', state.currentPrices['bitcoin']);
console.log('Ethereum цена:', state.currentPrices['ethereum']);

// Добавь монету в портфель и перезагрузи
// Цена должна обновиться в кэше!
```

**Эксперимент 3: Симуляция ошибки API**
```javascript
// Отключи интернет
// Или в api.js замени URL на невалидный
// Должно показаться сообщение об ошибке!
```

**Эксперимент 4: Тест пустого портфеля**
```javascript
import { portfolio } from './js/storage.js';
portfolio.clear();

// Перезагрузи страницу
// Должен показаться empty state!
```

## ☑️ Чек-лист готовности:

- [ ] Файл app.js создан
- [ ] Все модули импортированы
- [ ] Объект state создан и инициализирован
- [ ] Функция init работает корректно
- [ ] loadMarketData загружает и отображает 50 монет
- [ ] loadWatchlistData работает с избранным
- [ ] loadPortfolioData загружает портфель
- [ ] calculatePortfolioStats правильно считает прибыль
- [ ] calculatePortfolioValue суммирует стоимость
- [ ] Кэш цен обновляется при каждой загрузке
- [ ] Обработка ошибок работает (try/catch)
- [ ] В консоли видны сообщения о запуске
- [ ] Ты понимаешь весь поток от инициализации до отображения

---

# 🎮 ЭТАП 7: ОБРАБОТЧИКИ ДЕЙСТВИЙ (app.js - Часть 2)

## 🎯 Цель:
Создать функции-обработчики для действий пользователя: поиск, графики, избранное, портфель.

## 📋 Что нужно реализовать:

### Функция 1: Обработка поиска

**Название**: `handleSearch` (async)
**Параметры**: query (строка поиска)
**Что должна делать**:

1. Проверить query:
   - Если пустая строка или длина < 2:
     - Скрыть результаты: найти элемент 'searchResults', убрать класс 'show'
     - Выйти из функции (return)

2. try/catch обязателен!

3. Вызвать API поиска:
   ```javascript
   const results = await API.searchCoins(query.trim());
   ```

4. Получить контейнер результатов:
   ```javascript
   const resultsContainer = document.getElementById('searchResults');
   ```

5. Если результатов нет (length === 0):
   - Установить innerHTML:
     ```html
     <div class="empty-state"><p>Ничего не найдено</p></div>
     ```

6. Если есть результаты:
   - Очистить контейнер: `resultsContainer.innerHTML = ''`
   - Взять первые 10 результатов: `.slice(0, 10)`
   - Для каждого результата:
     - Создать элемент: `UI.createSearchResultItem(coin)`
     - Добавить в контейнер: `.appendChild(item)`

7. Показать результаты: добавить класс 'show'

8. При ошибке — вывести в консоль

### Функция 2: Показать график

**Название**: `showChart` (async)
**Параметры**: coinId
**Что должна делать**:

1. try/catch

2. Сохранить в state:
   ```javascript
   state.currentChartCoin = coinId;
   ```

3. Найти данные монеты:
   ```javascript
   const coin = [...state.marketData, ...state.watchlistData]
     .find(c => c.id === coinId);
   ```
   - Объясню: объединяем оба массива (spread) и ищем монету

4. Если не найдена — выйти (return)

5. Открыть модалку:
   ```javascript
   UI.toggleModal('chartModal', true);
   ```

6. Установить заголовок графика:
   ```javascript
   document.getElementById('chartTitle').textContent =
     `${coin.name} (${coin.symbol.toUpperCase()})`;
   ```

7. Загрузить данные графика:
   ```javascript
   await updateChartData(coinId, state.currentChartPeriod);
   ```

8. При ошибке — вывести в консоль

### Функция 3: Обновить данные графика

**Название**: `updateChartData` (async)
**Параметры**: coinId, days
**Что должна делать**:

1. try/catch

2. Получить данные:
   ```javascript
   const data = await API.getMarketChart(coinId, state.currentCurrency, days);
   ```

3. Проверить что данные есть:
   ```javascript
   if (!data || !data.prices) {
     console.error('No chart data');
     return;
   }
   ```

4. Создать/обновить график:
   ```javascript
   createPriceChart('priceChart', data.prices, 'Price');
   ```

5. Получить статистику:
   ```javascript
   const stats = getChartStats(data.prices);
   ```

6. Отобразить статистику:
   ```javascript
   UI.renderChartInfo(stats);
   ```

7. Обновить state:
   ```javascript
   state.currentChartPeriod = days;
   ```

8. При ошибке — вывести в консоль

### Функция 4: Показать окно добавления в портфель

**Название**: `showAddToPortfolio`
**Параметры**: coinId
**Что должна делать**:

1. Найти монету:
   ```javascript
   const coin = [...state.marketData, ...state.watchlistData]
     .find(c => c.id === coinId);
   ```

2. Если не найдена — выйти

3. Заполнить форму:
   - Найти поле 'coinName', установить value: `${coin.name} (${coin.symbol.toUpperCase()})`
   - Найти поле 'coinAmount', очистить value: `''`
   - Найти поле 'buyPrice', установить value: `coin.current_price`
   - Установить placeholder для buyPrice: `Текущая: ${UI.formatPrice(coin.current_price)}`

4. Сохранить данные в dataset кнопки:
   ```javascript
   const addBtn = document.getElementById('addToPortfolioBtn');
   addBtn.dataset.coinId = coin.id;
   addBtn.dataset.coinName = coin.name;
   addBtn.dataset.coinSymbol = coin.symbol;
   addBtn.dataset.coinImage = coin.image;
   addBtn.dataset.coinPrice = coin.current_price;
   ```

5. Открыть модалку:
   ```javascript
   UI.toggleModal('portfolioModal', true);
   ```

### Функция 5: Добавить в портфель

**Название**: `addToPortfolio`
**Что должна делать**:

1. Получить данные из формы:
   ```javascript
   const addBtn = document.getElementById('addToPortfolioBtn');
   const amount = parseFloat(document.getElementById('coinAmount').value);
   const buyPrice = parseFloat(document.getElementById('buyPrice').value)
                    || parseFloat(addBtn.dataset.coinPrice);
   ```

2. Валидация:
   ```javascript
   if (!amount || amount <= 0) {
     alert('Введите корректное количество');
     return;
   }
   ```

3. Создать объект монеты:
   ```javascript
   const coin = {
     id: addBtn.dataset.coinId,
     name: addBtn.dataset.coinName,
     symbol: addBtn.dataset.coinSymbol,
     image: addBtn.dataset.coinImage,
     amount: amount,
     buyPrice: buyPrice,
     currentPrice: parseFloat(addBtn.dataset.coinPrice)
   };
   ```

4. Добавить в портфель:
   ```javascript
   portfolio.add(coin);
   ```

5. Закрыть модалку:
   ```javascript
   UI.toggleModal('portfolioModal', false);
   ```

6. Перезагрузить портфель:
   ```javascript
   loadPortfolioData();
   ```

7. Переключиться на таб портфеля:
   ```javascript
   UI.switchTab('portfolio');
   ```

### Функция 6: Переключить избранное

**Название**: `toggleWatchlist`
**Параметры**: coinId
**Что должна делать**:

1. Проверить есть ли в избранном:
   ```javascript
   if (watchlist.has(coinId)) {
     watchlist.remove(coinId);
   } else {
     watchlist.add(coinId);
   }
   ```

2. Обновить UI:
   ```javascript
   loadMarketData();
   loadWatchlistData();
   ```

### Функция 7: Удалить из портфеля

**Название**: `removeFromPortfolio`
**Параметры**: coinId
**Что должна делать**:

1. Показать подтверждение:
   ```javascript
   if (confirm('Удалить из портфеля?')) {
     portfolio.remove(coinId);
     loadPortfolioData();
   }
   ```

## 💡 Подсказки:

- handleSearch должна быть async (API вызов)
- Используй spread оператор для объединения массивов
- dataset позволяет хранить данные в HTML элементах
- parseFloat превращает строку в число
- confirm() показывает диалог подтверждения

## 🔍 Как проверить:

**Тест поиска:**
```javascript
// Введи в поле поиска "bitcoin"
// Должен появиться выпадающий список результатов

// Кликни на результат
// Должен открыться график!
```

**Тест графика:**
```javascript
// Кликни на кнопку "📈 График" на любой карточке
// Модалка с графиком должна открыться

// Попробуй переключить периоды (24h, 7d, 30d, 1y)
// График должен обновляться!
```

**Тест добавления в портфель:**
```javascript
// Кликни "💼 Портфель" на карточке Bitcoin
// Модалка должна открыться с заполненными данными

// Введи количество: 0.5
// Кликни "Добавить"
// Должно переключиться на вкладку Portfolio
// Монета должна появиться в портфеле!
```

**Тест избранного:**
```javascript
// Кликни на звездочку ☆ на любой карточке
// Звездочка должна стать заполненной ⭐

// Перейди на вкладку Watchlist
// Монета должна там быть!

// Кликни звездочку снова — монета удалится
```

## 📊 Что ожидается:

### Поиск:
- Ввод менее 2 символов — ничего не происходит
- Ввод "bit" — показываются Bitcoin, BitTorrent и др.
- Клик на результат — открывается график
- Клик вне поиска — результаты скрываются

### График:
- Открывается модалка с названием монеты
- Загружается график за 7 дней (по умолчанию)
- Показывается статистика: макс, мин, изменение
- Переключение периодов обновляет график

### Портфель:
- Модалка показывает текущую цену
- Можно ввести свою цену покупки
- После добавления: автопереключение на вкладку Portfolio
- Монета отображается с прибылью/убытком

### Избранное:
- Клик на звездочку — монета добавляется
- Повторный клик — удаляется
- Данные сохраняются в LocalStorage
- После перезагрузки — список остается

## 🎯 ПАРОВОЗИК: ДОБАВЛЕНИЕ В ПОРТФЕЛЬ

```
1️⃣ Пользователь кликает "💼 Портфель" на карточке Bitcoin
          ↓
2️⃣ Событие клика ловится делегированием (обработчик будет в ЭТАПЕ 8)
          ↓
3️⃣ Вызывается showAddToPortfolio('bitcoin')
          ↓
4️⃣ Ищем монету в state.marketData или state.watchlistData
          ↓
5️⃣ Находим: { id: 'bitcoin', name: 'Bitcoin', current_price: 43250, ... }
          ↓
6️⃣ Заполняем поля формы:
   - coinName: "Bitcoin (BTC)"
   - coinAmount: "" (пустое)
   - buyPrice: "43250"
          ↓
7️⃣ Сохраняем данные в dataset кнопки "Добавить"
          ↓
8️⃣ UI.toggleModal('portfolioModal', true) — открываем модалку
          ↓
9️⃣ Пользователь вводит количество: 0.5
          ↓
🔟 Пользователь кликает "Добавить"
          ↓
1️⃣1️⃣ Вызывается addToPortfolio()
          ↓
1️⃣2️⃣ Читаем данные:
   - amount: 0.5
   - buyPrice: 43250
   - coinId, name, symbol, image из dataset
          ↓
1️⃣3️⃣ Валидация: amount > 0? ✅
          ↓
1️⃣4️⃣ Создаем объект coin:
   { id: 'bitcoin', name: 'Bitcoin', amount: 0.5, buyPrice: 43250, ... }
          ↓
1️⃣5️⃣ portfolio.add(coin) — сохранение в LocalStorage
          ↓
1️⃣6️⃣ UI.toggleModal('portfolioModal', false) — закрываем модалку
          ↓
1️⃣7️⃣ loadPortfolioData() — перезагрузка портфеля
          ↓
1️⃣8️⃣ API.getCoinsByIds(['bitcoin']) — получаем текущую цену
          ↓
1️⃣9️⃣ Рассчитываем прибыль: (43250 - 43250) * 0.5 = $0
          ↓
2️⃣0️⃣ UI.renderPortfolio() — отрисовка элемента портфеля
          ↓
2️⃣1️⃣ UI.switchTab('portfolio') — переключение на вкладку
          ↓
2️⃣2️⃣ Пользователь видит свою позицию! 💼
```

## ❓ Вопросы для самопроверки:

1. Зачем проверять длину query перед поиском?
2. Почему используем .slice(0, 10) для результатов?
3. Что делает spread оператор [...arr1, ...arr2]?
4. Зачем сохранять данные в dataset кнопки?
5. Что вернет parseFloat('abc')? Как это обработать?
6. Почему нужно confirm() перед удалением?
7. Зачем перезагружать данные после изменений?
8. Что произойдет если забыть toggleModal(false)?
9. Как работает .find() с массивом?
10. Почему нужно trim() для query?

## 🧪 Задания для экспериментов:

**Эксперимент 1: Попробуй разный поиск**
```javascript
// Поищи монеты:
// "eth" — Ethereum
// "bit" — Bitcoin, BitTorrent
// "xxx" — ничего не найдено
```

**Эксперимент 2: Проверь сохранение портфеля**
```javascript
// Добавь несколько монет в портфель
// Открой DevTools → Application → Local Storage
// Найди ключ 'crypto_tracker_portfolio'
// Посмотри JSON данные!

// Перезагрузи страницу
// Портфель должен остаться!
```

**Эксперимент 3: Тест валидации**
```javascript
// Попробуй добавить в портфель:
// - Количество: 0 (должна быть ошибка)
// - Количество: -5 (должна быть ошибка)
// - Количество: "abc" (должна быть ошибка)
// - Количество: 1.5 (должно работать)
```

**Эксперимент 4: Множественное добавление**
```javascript
// Добавь Bitcoin: 0.5 BTC по $40,000
// Добавь Bitcoin еще раз: 0.3 BTC по $42,000

// Должна быть ОДНА позиция с amount: 0.8
// Проверь в портфеле!
```

## ☑️ Чек-лист готовности:

- [ ] Функция handleSearch работает корректно
- [ ] Поиск показывает результаты
- [ ] Клик на результат открывает график
- [ ] showChart открывает модалку с графиком
- [ ] updateChartData обновляет график при смене периода
- [ ] showAddToPortfolio заполняет форму
- [ ] addToPortfolio сохраняет в портфель
- [ ] Валидация amount работает
- [ ] toggleWatchlist добавляет/удаляет из избранного
- [ ] removeFromPortfolio удаляет позицию
- [ ] Все данные сохраняются в LocalStorage
- [ ] После перезагрузки данные остаются

---

# 🔗 ЭТАП 8: ОБРАБОТКА СОБЫТИЙ (app.js - Часть 3)

## 🎯 Цель:
Настроить слушатели событий для интерактивности приложения.

## 📋 Что нужно реализовать:

### Функция: Настроить все обработчики

**Название**: `setupEventListeners`
**Что должна делать**:

Эта функция — центральное место для всех addEventListener. Вызывается один раз при инициализации.

### 1. Делегирование событий на document

```javascript
document.addEventListener('click', handleClick);
```

Создай функцию `handleClick(e)` которая будет обрабатывать ВСЕ клики (делегирование):

```javascript
function handleClick(e) {
  const target = e.target;

  // 1. Избранное
  if (target.classList.contains('btn-favorite')) {
    e.stopPropagation();
    const coinId = target.dataset.coinId;
    toggleWatchlist(coinId);
    return;
  }

  // 2. Действия с data-action
  if (target.dataset.action) {
    e.stopPropagation();
    const action = target.dataset.action;
    const coinId = target.dataset.coinId;

    switch (action) {
      case 'chart':
        showChart(coinId);
        break;
      case 'add-portfolio':
        showAddToPortfolio(coinId);
        break;
      case 'remove-portfolio':
        removeFromPortfolio(coinId);
        break;
    }
    return;
  }

  // 3. Клик на результат поиска
  if (target.closest('.search-result-item')) {
    const item = target.closest('.search-result-item');
    const coinId = item.dataset.coinId;
    document.getElementById('searchResults').classList.remove('show');
    showChart(coinId);
    return;
  }
}
```

**Почему делегирование?**
- Один обработчик вместо сотен
- Работает с динамическими элементами
- Экономит память

### 2. Поиск с debounce

```javascript
const searchInput = document.getElementById('searchInput');
searchInput.addEventListener('input', (e) => {
  // Очищаем предыдущий таймер
  clearTimeout(state.searchTimeout);

  // Устанавливаем новый таймер на 300мс
  state.searchTimeout = setTimeout(() => {
    handleSearch(e.target.value);
  }, 300);
});
```

**Что такое debounce?**
- Откладывает выполнение функции
- Если пользователь продолжает вводить — старый таймер отменяется
- Запрос отправляется только после паузы в 300мс
- Экономит API запросы!

### 3. Закрытие результатов поиска

```javascript
document.addEventListener('click', (e) => {
  // Если клик был НЕ внутри поиска
  if (!e.target.closest('.search-section')) {
    document.getElementById('searchResults').classList.remove('show');
  }
});
```

### 4. Смена валюты

```javascript
document.getElementById('currencySelect').addEventListener('change', (e) => {
  state.currentCurrency = e.target.value;
  settings.setCurrency(e.target.value);

  // Перезагрузить все данные в новой валюте
  loadMarketData();
  loadWatchlistData();
  loadPortfolioData();
});
```

### 5. Смена сортировки

```javascript
document.getElementById('sortSelect').addEventListener('change', (e) => {
  state.currentSort = e.target.value;
  loadMarketData();
});
```

### 6. Кнопка обновления

```javascript
document.getElementById('refreshBtn').addEventListener('click', () => {
  loadMarketData();
  loadWatchlistData();
  loadPortfolioData();
});
```

### 7. Пагинация

```javascript
document.getElementById('prevPage').addEventListener('click', () => {
  if (state.currentPage > 1) {
    state.currentPage--;
    loadMarketData();
    document.getElementById('pageInfo').textContent = `Страница ${state.currentPage}`;
  }
});

document.getElementById('nextPage').addEventListener('click', () => {
  state.currentPage++;
  loadMarketData();
  document.getElementById('pageInfo').textContent = `Страница ${state.currentPage}`;
});
```

### 8. Переключение темы

```javascript
document.getElementById('themeToggle').addEventListener('click', toggleTheme);
```

Создай функции для темы:

```javascript
function toggleTheme() {
  const currentTheme = settings.getTheme();
  const newTheme = currentTheme === 'light' ? 'dark' : 'light';
  settings.setTheme(newTheme);
  applyTheme();
}

function applyTheme() {
  const theme = settings.getTheme();
  document.documentElement.setAttribute('data-theme', theme);

  const themeIcon = document.querySelector('.theme-icon');
  if (themeIcon) {
    themeIcon.textContent = theme === 'light' ? '🌙' : '☀️';
  }
}
```

### 9. Переключение табов

```javascript
document.querySelectorAll('.tab-btn').forEach(btn => {
  btn.addEventListener('click', (e) => {
    const tab = e.target.dataset.tab;
    UI.switchTab(tab);

    // Загрузить данные для таба
    if (tab === 'watchlist') {
      loadWatchlistData();
    } else if (tab === 'portfolio') {
      loadPortfolioData();
    }
  });
});
```

### 10. Модалки

**Закрытие крестиком:**
```javascript
document.getElementById('closeChartModal').addEventListener('click', () => {
  UI.toggleModal('chartModal', false);
  destroyChart(); // Важно! Очищаем график
});

document.getElementById('closePortfolioModal').addEventListener('click', () => {
  UI.toggleModal('portfolioModal', false);
});
```

**Закрытие кликом на overlay:**
```javascript
document.querySelectorAll('.modal-overlay').forEach(overlay => {
  overlay.addEventListener('click', (e) => {
    // Клик был на overlay (не на modal-content)
    if (e.target === overlay) {
      const modal = overlay.closest('.modal');
      UI.toggleModal(modal.id, false);

      if (modal.id === 'chartModal') {
        destroyChart();
      }
    }
  });
});
```

### 11. Кнопки периода графика

```javascript
document.querySelectorAll('.chart-period-btn').forEach(btn => {
  btn.addEventListener('click', (e) => {
    const period = parseInt(e.target.dataset.period);

    // Убираем active у всех кнопок
    document.querySelectorAll('.chart-period-btn').forEach(b => {
      b.classList.remove('active');
    });

    // Добавляем active текущей
    e.target.classList.add('active');

    // Обновляем график
    if (state.currentChartCoin) {
      updateChartData(state.currentChartCoin, period);
    }
  });
});
```

### 12. Добавление в портфель

```javascript
document.getElementById('addToPortfolioBtn').addEventListener('click', addToPortfolio);
```

### 13. Автообновление

Создай функции:

```javascript
function startAutoUpdate() {
  // Очищаем предыдущий интервал если есть
  if (state.updateInterval) {
    clearInterval(state.updateInterval);
  }

  // Обновляем каждую минуту (UPDATE_INTERVAL = 60000мс)
  state.updateInterval = setInterval(() => {
    console.log('🔄 Auto-updating data...');
    loadMarketData();

    // Обновляем активный таб
    const activeTab = document.querySelector('.tab-btn.active');
    if (activeTab) {
      const tab = activeTab.dataset.tab;
      if (tab === 'watchlist') {
        loadWatchlistData();
      } else if (tab === 'portfolio') {
        loadPortfolioData();
      }
    }
  }, UPDATE_INTERVAL);
}

function stopAutoUpdate() {
  if (state.updateInterval) {
    clearInterval(state.updateInterval);
    state.updateInterval = null;
  }
}
```

### 14. Очистка при закрытии страницы

```javascript
window.addEventListener('beforeunload', () => {
  stopAutoUpdate();
});
```

### 15. Запуск приложения

В самом конце файла app.js:

```javascript
// Запуск приложения
init();
```

Или если хочешь дождаться загрузки DOM:

```javascript
document.addEventListener('DOMContentLoaded', init);
```

## 💡 Подсказки:

- Делегирование: один слушатель на document вместо множества
- e.stopPropagation() останавливает всплытие события
- closest() ищет ближайший родительский элемент
- clearTimeout() отменяет предыдущий таймер
- setInterval() выполняет функцию периодически
- clearInterval() останавливает интервал
- data-атрибуты доступны через dataset

## 🔍 Как проверить:

**Тест делегирования:**
```javascript
// Кликни на любую кнопку "График" или "Портфель"
// Должно сработать!

// Кликни на звездочку
// Должна добавиться в избранное

console.log('Обработчиков click на document:',
  getEventListeners(document).click.length);
// Должно быть всего 2-3 (делегирование!)
```

**Тест debounce:**
```javascript
// Открой консоль
// Быстро набери в поиске: "bitcoin"
// Запрос должен отправиться ТОЛЬКО ОДИН РАЗ
// После 300мс паузы

// Посчитай сколько запросов в Network tab
```

**Тест автообновления:**
```javascript
// После запуска приложения подожди 60 секунд
// В консоли должно появиться: "🔄 Auto-updating data..."
// Данные должны обновиться!

// Проверь state.updateInterval
console.log('Интервал ID:', state.updateInterval);
// Не должен быть null
```

**Тест модалок:**
```javascript
// Открой график
// Кликни вне модалки (на затемнение)
// Модалка должна закрыться

// Открой график снова
// Нажми Escape (если добавишь обработчик)
// Модалка должна закрыться
```

**Тест темы:**
```javascript
// Кликни на кнопку темы
// Тема должна переключиться
// Иконка должна измениться: 🌙 ↔ ☀️

// Перезагрузи страницу
// Тема должна сохраниться!
```

## 📊 Что ожидается:

### После setupEventListeners:
- ✅ Все кнопки работают
- ✅ Клики обрабатываются делегированием
- ✅ Поиск работает с задержкой
- ✅ Табы переключаются
- ✅ Модалки открываются/закрываются
- ✅ Тема переключается и сохраняется
- ✅ Пагинация работает
- ✅ Автообновление запущено

## 🎯 ПАРОВОЗИК: ОБРАБОТКА КЛИКА НА "ГРАФИК"

```
1️⃣ Пользователь кликает кнопку "📈 График" на карточке Bitcoin
          ↓
2️⃣ Браузер генерирует событие 'click'
          ↓
3️⃣ Событие всплывает до document
          ↓
4️⃣ Слушатель на document ловит событие
          ↓
5️⃣ handleClick(e) вызывается
          ↓
6️⃣ Проверка: target.dataset.action существует?
          ↓
7️⃣ Да! action = 'chart'
          ↓
8️⃣ e.stopPropagation() — останавливаем всплытие
          ↓
9️⃣ Получаем coinId из target.dataset.coinId = 'bitcoin'
          ↓
🔟 switch(action) → case 'chart'
          ↓
1️⃣1️⃣ Вызов showChart('bitcoin')
          ↓
1️⃣2️⃣ state.currentChartCoin = 'bitcoin'
          ↓
1️⃣3️⃣ Ищем монету в state.marketData
          ↓
1️⃣4️⃣ Находим: { id: 'bitcoin', name: 'Bitcoin', ... }
          ↓
1️⃣5️⃣ UI.toggleModal('chartModal', true) — открываем модалку
          ↓
1️⃣6️⃣ Устанавливаем заголовок: "Bitcoin (BTC)"
          ↓
1️⃣7️⃣ updateChartData('bitcoin', 7) — загружаем данные за 7 дней
          ↓
1️⃣8️⃣ API.getMarketChart('bitcoin', 'usd', 7)
          ↓
1️⃣9️⃣ Получены данные: { prices: [[...], [...], ...] }
          ↓
2️⃣0️⃣ createPriceChart('priceChart', prices, 'Price')
          ↓
2️⃣1️⃣ График отрисовывается на canvas
          ↓
2️⃣2️⃣ getChartStats(prices) — вычисляется статистика
          ↓
2️⃣3️⃣ UI.renderChartInfo(stats) — отображается статистика
          ↓
2️⃣4️⃣ Пользователь видит график! 📈
```

## ❓ Вопросы для самопроверки:

1. Что такое делегирование событий? В чем преимущество?
2. Что делает e.stopPropagation()?
3. Что делает e.preventDefault()?
4. В чем разница между target и currentTarget?
5. Что такое debounce? Зачем он нужен?
6. Как работает setTimeout?
7. Что делает clearTimeout?
8. Что такое setInterval? Чем отличается от setTimeout?
9. Зачем вызывать clearInterval перед созданием нового?
10. Что делает closest()?
11. Зачем проверять if (state.updateInterval) перед clearInterval?
12. Что произойдет если забыть stopPropagation?

## 🧪 Задания для экспериментов:

**Эксперимент 1: Посчитай обработчики**
```javascript
// В консоли:
console.log('Слушателей click:', getEventListeners(document).click.length);

// Теперь представь если бы на каждую из 50 карточек
// вешать отдельный слушатель...
// Вот почему делегирование круто!
```

**Эксперимент 2: Тест debounce**
```javascript
let requestCount = 0;

// Перехвати handleSearch:
const originalHandleSearch = handleSearch;
handleSearch = async function(query) {
  requestCount++;
  console.log(`Запрос #${requestCount}: "${query}"`);
  return originalHandleSearch.call(this, query);
};

// Быстро набери: "b-i-t-c-o-i-n"
// Сколько запросов? Должен быть только 1!
```

**Эксперимент 3: Проверь автообновление**
```javascript
// Открой консоль
// Запусти таймер:
let secondsPassed = 0;
setInterval(() => {
  secondsPassed++;
  console.log(`Прошло секунд: ${secondsPassed}`);
}, 1000);

// Дождись 60 секунд
// Должно появиться: "🔄 Auto-updating data..."
```

**Эксперимент 4: Множественные клики**
```javascript
// Быстро кликни 10 раз на кнопку "Обновить"
// Должно отправиться 10 запросов
// Все должны завершиться успешно

// Можно добавить debounce и для кнопки обновить!
```

**Эксперимент 5: Тест утечек памяти**
```javascript
// Открой Performance Monitor в DevTools
// Посмотри использование памяти

// Кликай на разные графики 20-30 раз
// Закрывай модалки

// Если забыть destroyChart():
// - Память будет расти!
// - Графики будут накладываться друг на друга!

// С destroyChart():
// - Память стабильна
```

## ☑️ Чек-лист готовности:

- [ ] Функция setupEventListeners создана
- [ ] Делегирование событий работает
- [ ] handleClick обрабатывает все типы кликов
- [ ] Поиск работает с debounce (300мс)
- [ ] Смена валюты перезагружает данные
- [ ] Смена сортировки работает
- [ ] Пагинация работает корректно
- [ ] Переключение темы работает
- [ ] Табы переключаются
- [ ] Модалки открываются/закрываются
- [ ] Кнопки периода графика работают
- [ ] Автообновление запущено (60 сек)
- [ ] stopAutoUpdate вызывается при закрытии страницы
- [ ] init() вызывается в конце файла
- [ ] Приложение полностью работает!

---

# 🎉 ЭТАП 9: ФИНАЛЬНОЕ ТЕСТИРОВАНИЕ И ЗАПУСК

## 🎯 Цель:
Проверить работу всего приложения, протестировать все функции, исправить баги.

## 📋 Чек-лист полного тестирования:

### 1. Запуск приложения

- [ ] Открой index.html в браузере через Live Server
- [ ] Консоль: должно быть '🚀 CryptoTracker starting...'
- [ ] Консоль: должно быть '✅ CryptoTracker ready!'
- [ ] Нет ошибок в консоли
- [ ] Загрузились 50 монет

### 2. Рыночные данные (вкладка Market)

- [ ] Отображаются 50 карточек криптовалют
- [ ] У каждой карточки: иконка, название, символ, ранг
- [ ] Отображается текущая цена
- [ ] Отображается изменение за 24ч (зеленое/красное)
- [ ] Отображается Market Cap, Volume, High, Low
- [ ] Звездочки избранного (☆) кликабельны
- [ ] Кнопки "График" и "Портфель" работают

### 3. Поиск

- [ ] Поле поиска реагирует на ввод
- [ ] При вводе < 2 символов — ничего не происходит
- [ ] При вводе "bit" — появляются результаты
- [ ] Результаты показываются с задержкой (debounce)
- [ ] Клик на результат открывает график
- [ ] Клик вне поиска закрывает результаты
- [ ] Поиск несуществующей монеты показывает "Ничего не найдено"

### 4. Избранное (Watchlist)

- [ ] Клик на ☆ превращает её в ⭐
- [ ] Монета добавляется в избранное
- [ ] Переход на вкладку Watchlist показывает монету
- [ ] Клик на ⭐ удаляет из избранного
- [ ] После перезагрузки страницы избранное сохраняется
- [ ] Счетчик на кнопке Watchlist обновляется
- [ ] Пустое избранное показывает empty state

### 5. График цен

- [ ] Клик на "График" открывает модалку
- [ ] Заголовок: название монеты и символ
- [ ] График отрисовывается
- [ ] По умолчанию показывается период 7 дней
- [ ] Кнопки периодов (1d, 7d, 30d, 1y) работают
- [ ] График обновляется при смене периода
- [ ] Статистика показывает: максимум, минимум, изменение
- [ ] Hover на графике показывает tooltip с ценой
- [ ] Закрытие крестиком работает
- [ ] Закрытие кликом на затемнение работает
- [ ] График разных монет открывается

### 6. Портфель

**Добавление:**
- [ ] Клик на "💼 Портфель" открывает модалку
- [ ] Название монеты заполнено автоматически
- [ ] Текущая цена подставляется в поле "Цена покупки"
- [ ] Ввод количества работает
- [ ] Кнопка "Добавить" работает
- [ ] После добавления переключается на вкладку Portfolio
- [ ] Позиция отображается в портфеле

**Отображение:**
- [ ] Показывается иконка, название, символ
- [ ] Показывается количество монет
- [ ] Показывается цена покупки и текущая цена
- [ ] Показывается общая стоимость
- [ ] Показывается прибыль/убыток в $ и %
- [ ] Прибыль зеленая, убыток красный
- [ ] Кнопка "Удалить" работает

**Сводка:**
- [ ] Общая стоимость портфеля отображается
- [ ] Прибыль/убыток отображается
- [ ] Изменение за 24ч отображается
- [ ] При добавлении второй монеты сводка обновляется
- [ ] Значения в header обновляются

**Множественное добавление:**
- [ ] Добавь Bitcoin: 0.5 BTC по $40,000
- [ ] Добавь Bitcoin еще раз: 0.3 BTC по $42,000
- [ ] Должна быть ОДНА позиция с amount: 0.8
- [ ] buyPrice должна обновиться

**Сохранение:**
- [ ] После перезагрузки страницы портфель остается
- [ ] Текущие цены обновляются при загрузке
- [ ] Прибыль пересчитывается

### 7. Настройки

**Валюта:**
- [ ] По умолчанию USD
- [ ] Смена на EUR работает
- [ ] Все цены конвертируются
- [ ] Символ валюты меняется ($ → €)
- [ ] После перезагрузки валюта сохраняется
- [ ] Портфель пересчитывается в новой валюте

**Сортировка:**
- [ ] По умолчанию: Market Cap (desc)
- [ ] Смена сортировки работает
- [ ] Список монет обновляется

**Тема:**
- [ ] По умолчанию: светлая
- [ ] Клик на 🌙 переключает на темную
- [ ] Иконка меняется на ☀️
- [ ] Цвета интерфейса меняются
- [ ] После перезагрузки тема сохраняется

### 8. Пагинация

- [ ] Кнопка "Назад" недоступна на странице 1
- [ ] Клик на "Вперед" загружает страницу 2
- [ ] Отображается "Страница 2"
- [ ] Загружаются другие 50 монет
- [ ] Кнопка "Назад" теперь доступна
- [ ] Возврат на страницу 1 работает

### 9. Автообновление

- [ ] Через 60 секунд в консоли: "🔄 Auto-updating data..."
- [ ] Данные рынка обновляются
- [ ] Если открыта вкладка Watchlist — она обновляется
- [ ] Если открыта вкладка Portfolio — она обновляется
- [ ] График НЕ обновляется автоматически (это правильно)

### 10. Кнопка "Обновить"

- [ ] Клик на кнопку обновляет все данные
- [ ] Цены обновляются
- [ ] Счетчики обновляются
- [ ] Портфель пересчитывается

### 11. Адаптивность (если есть время)

- [ ] Открой DevTools → Toggle Device Toolbar
- [ ] Проверь на iPhone (375px)
- [ ] Проверь на iPad (768px)
- [ ] Проверь на Desktop (1920px)
- [ ] Все элементы видны и кликабельны

### 12. Граничные случаи

**Пустые состояния:**
- [ ] Пустое избранное показывает empty state
- [ ] Пустой портфель показывает empty state
- [ ] Поиск без результатов показывает сообщение

**Ошибки:**
- [ ] Отключи интернет → должна показаться ошибка
- [ ] Включи интернет → нажми "Обновить" → должно заработать

**Валидация:**
- [ ] Попробуй добавить количество: 0 → должна быть ошибка
- [ ] Попробуй добавить количество: -5 → должна быть ошибка
- [ ] Попробуй добавить количество: "abc" → должна быть ошибка

**Производительность:**
- [ ] Открой 20 графиков подряд → не должно тормозить
- [ ] Добавь 10 монет в портфель → все должно работать быстро
- [ ] Переключай табы 50 раз → не должно лагать

## 🐛 Типичные баги и как их исправить:

### Баг 1: График не отображается
**Причина**: Не подключен Chart.js CDN
**Решение**: Проверь в index.html наличие `<script src="https://cdn.jsdelivr.net/npm/chart.js..."></script>`

### Баг 2: "Failed to fetch"
**Причина**: CORS или нет интернета
**Решение**: Используй Live Server, проверь интернет

### Баг 3: Данные не сохраняются
**Причина**: LocalStorage отключен
**Решение**: Проверь настройки браузера

### Баг 4: Графики накладываются
**Причина**: Забыл вызвать destroyChart()
**Решение**: Всегда уничтожай график перед созданием нового

### Баг 5: Поиск отправляет слишком много запросов
**Причина**: Нет debounce
**Решение**: Используй setTimeout и clearTimeout

### Баг 6: Портфель неправильно считает amount
**Причина**: В portfolio.add не складывается amount
**Решение**: При обновлении: `list[index].amount += coin.amount`

### Баг 7: Тема не применяется
**Причина**: Забыл вызвать applyTheme() в init()
**Решение**: Добавь вызов функции

## 🎯 ПАРОВОЗИК: ПОЛНЫЙ ЦИКЛ РАБОТЫ ПРИЛОЖЕНИЯ

```
1️⃣ Пользователь открывает index.html
          ↓
2️⃣ Браузер загружает HTML, CSS, JS модули
          ↓
3️⃣ app.js выполняется
          ↓
4️⃣ init() вызывается
          ↓
5️⃣ applyTheme() — применяется тема из LocalStorage
          ↓
6️⃣ loadMarketData() — API запрос к CoinGecko
          ↓
7️⃣ 50 монет загружены и отрисованы
          ↓
8️⃣ loadWatchlistData() — чтение из LocalStorage + API
          ↓
9️⃣ Избранное отображено (если есть)
          ↓
🔟 loadPortfolioData() — чтение портфеля + текущие цены
          ↓
1️⃣1️⃣ Портфель отображен с прибылью/убытками
          ↓
1️⃣2️⃣ setupEventListeners() — все обработчики настроены
          ↓
1️⃣3️⃣ startAutoUpdate() — запущен интервал 60 сек
          ↓
1️⃣4️⃣ Приложение полностью готово! ✅
          ↓
          ↓ (пользователь взаимодействует)
          ↓
1️⃣5️⃣ Клик на кнопку → делегирование ловит событие
          ↓
1️⃣6️⃣ handleClick определяет тип действия
          ↓
1️⃣7️⃣ Вызывается соответствующая функция
          ↓
1️⃣8️⃣ API запрос (если нужно) → обновление state → UI рендеринг
          ↓
1️⃣9️⃣ LocalStorage обновляется (для избранного/портфеля)
          ↓
2️⃣0️⃣ Пользователь видит результат мгновенно
          ↓
          ↓ (через 60 секунд)
          ↓
2️⃣1️⃣ Автообновление срабатывает
          ↓
2️⃣2️⃣ Цены обновляются в фоне
          ↓
2️⃣3️⃣ UI обновляется с новыми данными
          ↓
          ↓ (пользователь закрывает страницу)
          ↓
2️⃣4️⃣ beforeunload → stopAutoUpdate()
          ↓
2️⃣5️⃣ Интервал остановлен, память освобождена
          ↓
2️⃣6️⃣ Данные остались в LocalStorage
          ↓
          ↓ (пользователь возвращается)
          ↓
2️⃣7️⃣ Цикл начинается заново с init()
          ↓
2️⃣8️⃣ Все сохраненные данные загружаются! 🎉
```

## ❓ Финальные вопросы для самопроверки:

1. Как работает весь поток от API до UI?
2. Зачем нужен объект state?
3. Как работает делегирование событий?
4. Что такое debounce и где он применяется?
5. Как сохраняются данные между сеансами?
6. Почему важно уничтожать график перед созданием нового?
7. Как работает автообновление?
8. Почему используются try/catch?
9. Как рассчитывается прибыль/убыток в портфеле?
10. Что произойдет если API недоступен?

## 🎓 Что ты изучил в этом проекте:

### JavaScript концепции:
- ✅ ES6 модули (import/export)
- ✅ Async/Await
- ✅ Fetch API
- ✅ Promises
- ✅ Array methods (map, filter, find, forEach, slice, some)
- ✅ Object destructuring
- ✅ Spread operator
- ✅ Template literals
- ✅ Arrow functions
- ✅ Default parameters
- ✅ Try/Catch error handling

### DOM и браузер:
- ✅ querySelector / getElementById
- ✅ createElement / appendChild
- ✅ innerHTML / textContent
- ✅ classList (add/remove/toggle)
- ✅ dataset (data-атрибуты)
- ✅ Event listeners
- ✅ Event delegation
- ✅ Event bubbling и stopPropagation
- ✅ LocalStorage (getItem/setItem)
- ✅ JSON.stringify / JSON.parse

### Архитектурные паттерны:
- ✅ Модульная архитектура
- ✅ Разделение ответственности (Separation of Concerns)
- ✅ State management
- ✅ API layer
- ✅ UI layer
- ✅ Storage layer
- ✅ Event handling layer

### Работа с API:
- ✅ REST API запросы
- ✅ URLSearchParams
- ✅ Query parameters
- ✅ Response handling
- ✅ Error handling
- ✅ Data transformation

### UI/UX паттерны:
- ✅ Loading states
- ✅ Error states
- ✅ Empty states
- ✅ Modal windows
- ✅ Tabs
- ✅ Search with debounce
- ✅ Auto-refresh
- ✅ Pagination

### Продвинутые техники:
- ✅ Debouncing
- ✅ setInterval / setTimeout
- ✅ Memory management (cleanup)
- ✅ Chart.js integration
- ✅ Gradient canvas
- ✅ Data caching

## 🚀 Что дальше?

### Улучшения которые можно добавить:

**Уровень 1 (легко):**
- [ ] Добавить кнопку "Наверх" при скролле
- [ ] Добавить анимации при загрузке
- [ ] Добавить звук при добавлении в портфель
- [ ] Добавить экспорт портфеля в CSV
- [ ] Добавить калькулятор конвертации

**Уровень 2 (средне):**
- [ ] Price Alerts — уведомления когда цена достигает порога
- [ ] Сравнение криптовалют на одном графике
- [ ] История транзакций портфеля
- [ ] Фильтры по капитализации/объему
- [ ] Интеграция с News API

**Уровень 3 (сложно):**
- [ ] Сделать PWA (работа офлайн)
- [ ] Добавить аутентификацию
- [ ] Синхронизация портфеля между устройствами
- [ ] Продвинутая аналитика портфеля
- [ ] Интеграция с реальными биржами (API ключи)

## 🎉 ПОЗДРАВЛЯЮ!

Ты создал полноценное приложение для трекинга криптовалют с:
- 📊 Реальными данными из API
- 💾 Сохранением в браузере
- 📈 Интерактивными графиками
- 💼 Управлением портфелем
- ⭐ Избранным
- 🎨 Темной/светлой темой
- 🔄 Автообновлением

Это проект который можно показать в портфолио!

## ☑️ Финальный чек-лист:

- [ ] Все 6 JS файлов созданы и работают
- [ ] Приложение запускается без ошибок
- [ ] Все функции протестированы
- [ ] Данные сохраняются между сеансами
- [ ] Автообновление работает
- [ ] Графики отображаются корректно
- [ ] Портфель считает прибыль правильно
- [ ] Нет утечек памяти
- [ ] Код чистый и понятный
- [ ] Ты понимаешь КАК ВСЕ РАБОТАЕТ!

---

## 🎊 ПРОЕКТ ЗАВЕРШЕН!

Ты только что создал сложное многомодульное приложение с нуля, используя только Vanilla JavaScript! Это огромное достижение! 🚀

Теперь ты умеешь:
- Работать с REST API
- Управлять состоянием приложения
- Создавать модульную архитектуру
- Работать с внешними библиотеками
- Обрабатывать события эффективно
- Сохранять данные
- И многое другое!

**Следующий шаг**: Покажи этот проект ментору и обсуди что можно улучшить!

---

**Good luck! 💎🚀**

